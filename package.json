{
  "name": "animated-icons",
  "version": "0.0.0",
  "private": true,
  "description": "Animated SVG icon library for Vue 3",
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "build": "pnpm --filter core build && pnpm --filter docs build",
    "build:core": "pnpm --filter core build",
    "build:docs": "pnpm --filter docs build",
    "release": "pnpm --filter core build && pnpm --filter core publish --access public"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
